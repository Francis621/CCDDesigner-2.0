# CCDDesigner 2.0 总体架构与 P0/P1 规格综合评审报告

| 文档属性 | 内容 |
| :--- | :--- |
| **报告编号** | CCD-REV-REPORT-2.0-001 |
| **评审版本** | V1.0 (正式评审基线) |
| **评审日期** | 2026年9月15日 |
| **上位依据** | 《CCDDesigner 2.0 产品开发说明书》（CCD-DEV-SPEC-2.0-001）<br>《CCDDesigner 2.0 产品功能架构与模块设计说明书》（CCD-ARCH-FUNC-2.0-001） |
| **评审对象** | 1. 《CCDDesigner 2.0 系统总体架构设计方案》<br>2. D01 (数据字典), D02 (状态机/PBAC), D03 (SysON/Flexo适配), D06 (MinIO制品/CAD), D04 (参数DAG/K8s仿真), D07 (PG数字主线), D09 (API/Outbox) 规格包<br>3. 官方 ADR 记录 (ADR-0001 ~ ADR-0011) |
| **评审结论** | **评审通过 (Approved to Implement)**，技术闭环严密，具备进入代码工程落地条件。 |

---

## 1. 评审背景与评估方法

本评审针对 CCDDesigner 2.0 平台总体架构及 P0（集成验证）、P1（系统设计闭环）的全部核心设计规格进行全面交叉复核。评估重点聚焦于：
1. **上位依据与约束遵从度**：核验是否 100% 满足《产品开发说明书》中的 6 大核心设计目标与 8 项强制冻结 ADR；
2. **验收测试用例覆盖度**：逐一复核 AT-01 ~ AT-07、AT-10、AT-20 等关键验收用例在物理表结构、算法与协议层面的闭环支撑；
3. **架构非功能性指标（SLA）**：评估万级 BOM 展开、百万级关系图遍历、大文件分片传输的可行性与容错能力；
4. **代码生成前置就绪度**：确认领域实体、接口协议、依赖组件与数据库 DDL 是否达到可直接编码的工程颗粒度。

---

## 2. 核心架构与规格闭环性逐项复核

### 2.1 架构原则与 ADR 遵从性复核矩阵

| 序号 | 架构准则 / ADR 冻结项 | 对应规格与落地章节 | 评审核验结论 |
| :--- | :--- | :--- | :--- |
| **ADR-0001** | **工具链版本物理绑定** | D03 第 1 节、D04 第 1 节、ADR-0001 | **合格**。明确锁定了 SysON 2026.2、OpenSysML 0.41.0、Flexo 2.4.1 及 OpenModelica 1.22.0 的精确版本与 SHA-256 镜像摘要。 |
| **ADR-0002** | **模型转换准入硬阻断** | D03 第 4.2 节、AT-02 | **合格**。设计了 `CompatibilityProfile` 白名单机制，对不支持语法返回 HTTP 422 并附带 `DiagnosticReport`，杜绝静默截断。 |
| **ADR-0003** | **Flexo 暂存分支隔离** | D03 第 4.3 节、AT-01 | **合格**。设计了 `urn:ccdd:staging:*` 与 `urn:ccdd:production:*` 命名空间分离，未审批前生产环境绝对不可见。 |
| **ADR-0004** | **仿真模板化填报策略** | D04 第 1 节、第 3.2 节 | **合格**。首期明确采用标准物理组件参数填报式求解，不搞在线自由微分方程拓扑生成，工程落地可行性高。 |
| **ADR-0005** | **零部件 FFF 互换性准则** | D01 第 1 节、ADR-0005 | **合格**。形位、配合或功能改变必须新建 `partMasterId`，禁止滥用升版替代新件。 |
| **ADR-0006** | **连接器准入与主写权冻结**| D06 第 5.1 节、ADR-0006 | **合格**。明确物料编号/版本/状态归 PLM 主写，几何尺寸/质量归 CAD 主写，签入时实施防篡改反击覆盖。 |
| **ADR-0007** | **性能 SLA 量化基准** | 总体方案第 8.2 节、ADR-0007 | **合格**。将页面响应与异步求解解耦，万级 BOM 走异步队列，百万关系图推演施加 5 层与 1000 节点硬限制。 |
| **ADR-0008** | **证据复用与豁免政策** | D04 第 1 节、第 3.4 节 | **合格**。求解成功不等于验证通过，设计升版后旧 PASS 严禁自动继承，必须由专职审核人独立签署适用性判定。 |
| **ADR-0009** | **Spring Boot 模块化单体**| 总体方案第 3 节、ADR-0009 | **合格**。P0/P1 采用模块化单体保障本地 ACID 发布事务，避免过早微服务化带来的分布式事务泥潭。 |
| **ADR-0010** | **PostgreSQL 承载数字主线**| D07 第 3 节、第 4 节、ADR-0010 | **合格**。彻底排除了 Neo4j，基于 PostgreSQL 复合双向索引与 `WITH RECURSIVE` 递归遍历，带环路防死锁。 |
| **ADR-0011** | **K8s 容器化动态调度** | D04 第 4.3 节、ADR-0011 | **合格**。采用 Kubernetes Job 动态拉起独立 Pod，实现环境隔离、版本锁定、迟到防污染与超时硬熔断（600s）。 |

---

### 2.2 核心验收测试用例（AT）全覆盖复核

| 用例编号 | 用例核心场景 | 对应支撑规格 | 物理保障机制复核说明 |
| :--- | :--- | :--- | :--- |
| **AT-01** | 标准子集 SysML v2 模型发布 | D03 | 两阶段提交（2PC）保证 Flexo Commit、MinIO 制品与 PostgreSQL 发布记录原子生效。 |
| **AT-02** | 未准入语法导入硬阻断 | D03 | `CompatibilityProfileValidator` 递归扫描 AST，生成包含行号与修复建议的结构化诊断报告。 |
| **AT-03** | 元素重命名移动后历史精准解析 | D03 | 采用 $\text{ModelElementRef}$ 四元组稳定键，历史基线引用只读命中特定 CommitID，不依赖显示路径。 |
| **AT-04** | Flexo 成功但 MinIO 失败回滚 | D03, D06 | 协调器异步下发 `DROP GRAPH staging`，发布状态标记 `FAILED`，禁止假冒成功，支持幂等重试。 |
| **AT-05** | 仿真与实测结果隔离留存 | D04 | `sys_parameter_value_records` 采用参数角色隔离，仿真与实测值作为独立实体，禁止覆写需求限值。 |
| **AT-06** | 历史仿真离线复现包可重复解算 | D04 | 归档完整 `.mo` 模型、参数 JSON 与环境镜像 SHA-256，实测重复解算数值误差 $\le 0.1\%$。 |
| **AT-07** | 伺服仿真成功不自动等价于 PASS | D04 | 状态机硬隔离，Run `SUCCEEDED` 仅生成证据，必须由具备资质的工程师签署 `VerificationAssessment`。 |
| **AT-10** | 主轴功率变更推演波及因果链 | D07 | `WITH RECURSIVE` 沿 `allocatedTo` 和 `satisfies` 毫秒级返回驱动器物料、轴承分析等完整因果树。 |
| **AT-20** | 高频参数连续变更防抖合并 | D04 | 3.0s 滑动防抖合并窗口（Debounce Window），5 次事件合并为 1 次下发，求解写回阻断二次循环。 |

---

## 3. 潜在架构风险分析与应对预案

在本次综合评审中，识别出以下 3 项实施阶段的潜在风险，并已制定工程应对预案：

### 风险 1：外部 Flexo RDF 服务网络抖动或重启导致暂存图残留
- **风险描述**：两阶段发布过程中，若在审批驳回阶段 Flexo 服务恰好停机，`DROP GRAPH` 命令可能丢失，导致暂存隔离命名空间中残留孤儿候选 Commit。
- **应对预案**：
  1. 在 M30 平台管理模块中设计**孤儿图定时巡检任务（Orphan Staging Graph Cleaner）**；
  2. 每天凌晨比对 `sys_model_releases` 中处于终态（`FAILED`/`OBSOLETE`）的记录，对 Flexo 中存活超过 72 小时的临时图执行幂等强制清理（`DROP SILENT GRAPH`）。

### 风险 2：大规模仿真任务突发导致 Kubernetes 节点算力枯竭
- **风险描述**：多位工程师并发执行机床整机热力学仿真，可能瞬间拉起数十个 OMC 计算 Pod，导致集群 CPU/内存过载。
- **应对预案**：
  1. 在 M10 调度器中建立基于数据库行级排队的限流网关（`ConcurrencyLimiter`，单项目最大并发数可配，默认 5 个并发 Run）；
  2. 超出限额的 Job 自动进入 `QUEUED` 排队等待，前序任务释放资源后由调度器按先进先出（FIFO）唤醒。

### 风险 3：PostgreSQL 递归图遍历在异常循环数据下的 CPU 飙升风险
- **风险描述**：若人为绕过约束在底表插入死循环引用，极端情况下可能耗尽数据库连接。
- **应对预案**：
  1. SQL 层面双重熔断：`array_append` 历史路径去重 + `depth < :maxDepth` 硬上限 + `LIMIT 1001`；
  2. 应用层超时配置：图查询服务强制配置数据库查询语句超时时间（`statement_timeout = 5000ms`），超时自动熔断并返回优雅降级提示。

---

## 4. 代码工程骨架建设方案（Scaffolding Plan）

为承接上述架构与规格，系统后端工程按照 **Spring Boot 模块化单体（Modular Monolith）** 规范进行脚手架搭建：

```
backend/
├── pom.xml                                   # 父 POM: 统一依赖版本 (Java 17, Spring Boot 3.2.x, PostgreSQL, MinIO, Fabric8)
├── ccdd-common/                              # 公共核心: 上下文拦截器, Snowflake ID, ModelElementRef, 统一返回体与异常
├── ccdd-security-pbac/                       # 安全切面: PBAC 动态多维授权, SoD 职责分离前置拦截器 (D02)
├── ccdd-outbox/                              # 事务性发件箱: sys_outbox_events 轮询投递, 消费端 Inbox 幂等切面 (D09)
├── ccdd-storage-minio/                       # 制品存储底座: MinIO S3 分片上传, SHA-256 强校验, 水印引擎适配 (D06)
├── ccdd-model-flexo/                         # 模型适配器: SysML v2 AST 准入校验, Flexo SPARQL 1.1 客户端 (D03)
├── ccdd-simulation-worker/                   # 仿真调度引擎: Kubernetes Job 动态调度, OpenModelica 容器交互 (D04)
├── ccdd-thread-graph/                        # 数字主线引擎: PostgreSQL 递归 CTE 图查询, 影响面推演服务 (D07)
└── ccdd-application/                         # 单体启动入口: SpringBootApplication, Flyway 数据库迁移脚本目录
```

---

## 5. 评审结论与后续行动指令

1. **评审结论**：**总体架构与 P0/P1 专项规格（D01~D07, D09）设计完备、逻辑自洽、边界清晰，一致同意通过评审，批准进入代码工程落地阶段！**
2. **后续首要任务**：
   - 搭建 `backend/` Maven 父子多模块工程骨架；
   - 汇总并生成全套 Flyway 数据库初始化迁移脚本（`V1.0.0__init_ccdd_core_schema.sql`）；
   - 编写 `ccdd-common` 与 `ccdd-outbox` 核心基石代码。
